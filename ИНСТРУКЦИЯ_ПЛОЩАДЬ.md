# 📐 Инструкция: Анализ оптимальной площади магазина

## 📁 Подготовка данных

### Шаг 1: Подготовьте файл с площадями

Создайте Excel файл (например, `store.xlsx`) со следующей структурой:

| Магазин (адрес) | Площадь (м²) |
|-----------------|--------------|
| Магазин 1       | 150          |
| Магазин 2       | 200          |
| ...             | ...          |

**Важно:**
- Колонка 1: Адрес/название магазина (должно совпадать с данными в `final_flat_clean.xlsx`)
- Колонка 2: Торговая площадь в м²

### Шаг 2: Положите файл в папку проекта

```bash
/home/user/store/
├── store_analytics.ipynb      # Основной notebook
├── store.xlsx                  # ← ВАШ ФАЙЛ С ПЛОЩАДЯМИ
├── final_flat_clean.xlsx       # Данные о продажах
└── optimal_area_analysis.py    # Готовый код (для справки)
```

---

## 🚀 Запуск анализа

### Вариант 1: Скопировать весь код в notebook

1. Откройте `store_analytics.ipynb`
2. Создайте новую ячейку после **Блока 3**
3. Скопируйте код из файла `optimal_area_analysis.py`
4. **Измените строку 27:** `STORE_AREA_FILE = 'store.xlsx'` на имя вашего файла
5. Запустите ячейку

### Вариант 2: Запустить отдельным скриптом

```python
# В новой ячейке notebook выполните:
%run optimal_area_analysis.py
```

---

## 📊 Что вы получите

### 4 метода расчёта оптимальной площади:

#### **Метод 1: Производительность на м²**
- Топ-5 магазинов по выручке/м² и прибыли/м²
- Оптимальная площадь = средняя площадь лидеров
- График: scatter выручки/м² от площади

#### **Метод 2: Нелинейная регрессия**
- Модели: линейная, квадратичная, кубическая
- Точка максимума функции выручки от площади
- Точка убывающей отдачи (где прирост <10%)
- График: кривая регрессии с точкой оптимума

#### **Метод 3: Предельная эффективность**
- Расчёт предельной выручки (∂Revenue/∂Area)
- Площадь с максимальной предельной выручкой
- График: предельная эффективность по площади

#### **Метод 4: Кластеризация + бенчмаркинг**
- Деление магазинов на: малые, средние, крупные
- Лидеры в каждом кластере
- Оптимум = средняя площадь лидеров
- График: box plot по кластерам

### Итоговая сводка:

```
ФИНАЛЬНАЯ РЕКОМЕНДАЦИЯ:
• Оптимальная площадь: 175 м² (медиана всех методов)
• Рекомендуемый диапазон: 150-200 м² (25-75 перцентиль)

РЕКОМЕНДАЦИИ ПО МАГАЗИНАМ:
🔴 Слишком малая:
  • Магазин 5: 80 м² → расширить на 95 м²
  • Магазин 12: 100 м² → расширить на 75 м²

🟠 Слишком большая:
  • Магазин 18: 300 м² → сократить на 125 м²
```

### Визуализации:

1. Scatter с цветом по прибыли/м² (метод 1)
2. Кривые регрессии (метод 2)
3. График предельной выручки (метод 3)
4. Box plot по кластерам (метод 4)
5. **Итоговая карта** с зоной оптимума

---

## 🔧 Решение проблем

### Ошибка: "Файл не найден"

```python
# Проверьте путь к файлу:
import os
print(os.getcwd())  # Текущая директория
print(os.listdir())  # Список файлов

# Если файл в другой папке, укажите полный путь:
STORE_AREA_FILE = '/full/path/to/store.xlsx'
```

### Ошибка: "Для N магазинов не найдена площадь"

**Причина:** Названия магазинов не совпадают

**Решение:** Код автоматически пытается сопоставить по номеру магазина. Если не помогло:

```python
# Посмотрите названия в обеих таблицах:
print("Магазины в продажах:")
print(df['Магазин'].unique())

print("\nМагазины в файле площадей:")
print(df_area['Магазин_адрес'].unique())

# Создайте mapping вручную:
mapping = {
    'ул. Ленина, 10': 'Магазин 1',
    'пр. Мира, 25': 'Магазин 2',
    # ...
}
df_area['Магазин'] = df_area['Магазин_адрес'].map(mapping)
```

### Ошибка: "Недостаточно данных для кластеризации"

**Причина:** Меньше 3 магазинов

**Решение:** Уменьшите количество кластеров в методе 4:

```python
# Измените строку с pd.qcut:
store_with_area['Размер_кластер'] = pd.qcut(
    store_with_area['Площадь_м2'],
    q=2,  # ← БЫЛО: 3
    labels=['Малые', 'Крупные'],  # ← БЫЛО: ['Малые', 'Средние', 'Крупные']
    duplicates='drop'
)
```

---

## 📌 Примечания

- Минимум 5-7 магазинов для надёжных результатов
- Для точной регрессии нужна вариативность площадей (не все магазины одинакового размера)

---

## 📞 Поддержка

При возникновении вопросов:
1. Проверьте формат файла (Excel, 2-3 колонки)
2. Убедитесь, что названия магазинов совпадают
3. Посмотрите примеры в коде (раздел "Проверка успешности мержа")
